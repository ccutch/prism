/*
Package prism implements the functions underlying both the cli and api
server for https://prism-api.made-by-connor.com.

You can get started by running prisma deploy

	func main() {
		if err := prisma.Deploy("./website"); err != nil {
			panic(err)
		}
		fmt.Println("Success: " + p.Status())
	}
*/
package prism
